SQL: SELECT SUM(OL_AMOUNT) FROM ORDER_LINE WHERE OL_O_ID = ? AND OL_D_ID = ? AND OL_W_ID = ?;
COST: 2919.0
PLAN:

RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: sum
  INDEX SCAN of "ORDER_LINE" using "IDX_ORDER_LINE_TREE" (unique-scan covering)

