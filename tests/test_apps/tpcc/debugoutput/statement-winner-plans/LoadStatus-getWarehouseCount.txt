SQL: SELECT COUNT (*) FROM WAREHOUSE;
COST: 6000000.0
PLAN:

RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: sum
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    AGGREGATION ops: count(*)
     SEQUENTIAL SCAN of "WAREHOUSE"

